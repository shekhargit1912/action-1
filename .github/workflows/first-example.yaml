name: My first WorkFlow 

on: push 

jobs: 
    first_job: 
        runs-on: ubuntu-latest
        steps:
        - name: Checkout
          uses: actions/checkout@v6.0.2

        - name: welcome messages
          run: | 
            echo "first gitctun action job"
            ls -ltra
            cat README.md

          
        
        # - name: list files
        #   run: ls 

        # - name: read the files
        #   run : cat README.md
